{{{bodyMethodTsComments}}}{{methodName}}({{>operationParameters}}): Observable<{{{resultType}}} | null> {

    return this.{{responseMethodName}}(params).pipe(
      map((r) => r.body as {{{resultType}}})
    );
  }
